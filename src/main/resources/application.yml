spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_digiex
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  servlet:
    context-path: /api
springdoc:
  swagger-ui:
    path: /swagger-ui.html

